const startName = '/fe-diploma-ticket-booking-system';

const links = {
   main: startName,
   trains: `${startName}/trains`,
   seats: `${startName}/seats`,
   passengers: `${startName}/passengers`,
   paymentOptions: `${startName}/paymentOptions`,
   confirmOrder: `${startName}/confirmOrder`,
   success: `${startName}/success`,
};

export default links;
